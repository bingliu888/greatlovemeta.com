"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
function defaultStartTime(){return new Date(Date.now()+3600000).toISOString().slice(0,16)}
export function ClassCreateForm({lang}:{lang:"en"|"zh"}){const zh=lang==="zh",router=useRouter(),[busy,setBusy]=useState(false),[error,setError]=useState(""),[startsAt,setStartsAt]=useState(defaultStartTime);
async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");try{const response=await fetch("/api/classes",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(event.currentTarget).entries()))}),result=await response.json().catch(()=>({}))as{code?:string;error?:string};if(response.ok&&result.code){router.push(`/${lang}/classes/${result.code}`);return}setError(result.error||(zh?"无法创建课程":"Unable to create class"))}catch{setError(zh?"无法创建课程":"Unable to create class")}finally{setBusy(false)}}
return <main className="class-create-page"><form className="class-form" onSubmit={submit}><label>{zh?"课程标题":"Class title"}<input name="title" required minLength={3} maxLength={120}/></label><label>{zh?"主题":"Subject"}<input name="subject" maxLength={80}/></label><label>{zh?"介绍":"Description"}<textarea name="description" maxLength={2000}/></label><div><label>{zh?"课堂类型":"Class type"}<select name="classType" defaultValue="public"><option value="public">{zh?"公课":"Public"}</option><option value="trial">{zh?"试课":"Trial"}</option><option value="private">{zh?"私课":"Private"}</option></select></label><label>{zh?"直播模式":"Streaming"}<select name="streamingMode" defaultValue="video"><option value="video">{zh?"音频与视频":"Audio / Video"}</option><option value="audio">{zh?"仅音频":"Audio only"}</option></select></label></div><div><label>{zh?"开始时间":"Start time"}<input name="startsAt" type="datetime-local" required value={startsAt} onChange={e=>setStartsAt(e.target.value)}/></label><label>{zh?"时长（分钟）":"Duration (minutes)"}<input name="durationMinutes" type="number" min={15} max={480} defaultValue={60}/></label></div><div><label>{zh?"试听分钟":"Trial minutes"}<input name="trialMinutes" type="number" min={5} max={1440} defaultValue={30}/></label><label>{zh?"学费（美元）":"Tuition (USD)"}<input name="tuition" type="number" min={1} step="0.01" defaultValue={99}/></label></div><label>{zh?"私课邀请邮箱":"Private invitation emails"}<textarea name="invites" placeholder="name@example.com, another@example.com"/></label>{error&&<p role="alert">{error}</p>}<button disabled={busy}>{busy?(zh?"正在创建…":"Creating…"):(zh?"创建课程":"Create class")}</button></form></main>}
